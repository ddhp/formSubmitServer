html
  include ./includes/head.jade
  body(ng-app='myApp')
    div(class='container')
      include ./includes/deps.jade
      div(ng-controller='IndexCtrl')
        h1!= message
        p Index page
        p!= name
